#include "main.h"
#include<stdio.h>
/**
 * main - prints all arguments it receives.
 * @argc: num of arg.
 * @argv: array of arg.
 *
 * Return: 0.
 */
int main(int argc, char *argv[])
{
while (argc = 1)
do {
argc++;
printf("%d\n", argc);
return (0);
}
}
